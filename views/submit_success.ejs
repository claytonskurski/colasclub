<!-- Event Header -->
<div class="success-header">
    <div class="container text-center py-5">
        <div class="success-icon-container">
            <i class="fas fa-check-circle success-icon"></i>
        </div>
        <h1 class="success-title">Event Submission Successful!</h1>
        <p class="success-subtitle">Your adventure idea has been received</p>
    </div>
</div>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="success-card">
                <div class="success-content">
                    <div class="success-message">
                        <h2 class="message-title">Thank You!</h2>
                        <p class="message-text">Your event idea has been submitted successfully and is now under review.</p>
                        <p class="message-subtext">We'll review your submission and get back to you soon. Keep an eye on your email for updates!</p>
                    </div>
                    
                    <div class="next-steps">
                        <h3 class="steps-title">What happens next?</h3>
                        <div class="steps-list">
                            <div class="step-item">
                                <div class="step-icon">
                                    <i class="fas fa-search"></i>
                                </div>
                                <div class="step-content">
                                    <h4>Review Process</h4>
                                    <p>Our team will review your event details and contact information</p>
                                </div>
                            </div>
                            <div class="step-item">
                                <div class="step-icon">
                                    <i class="fas fa-envelope"></i>
                                </div>
                                <div class="step-content">
                                    <h4>Email Confirmation</h4>
                                    <p>You'll receive an email with next steps within 2-3 business days</p>
                                </div>
                            </div>
                            <div class="step-item">
                                <div class="step-icon">
                                    <i class="fas fa-calendar-plus"></i>
                                </div>
                                <div class="step-content">
                                    <h4>Event Publishing</h4>
                                    <p>Once approved, your event will be published for the community to join</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="action-buttons">
                        <a href="/events" class="btn btn-primary btn-lg">
                            <i class="fas fa-calendar me-2"></i>Browse Events
                        </a>
                        <a href="/submit_event" class="btn btn-outline-primary btn-lg">
                            <i class="fas fa-plus me-2"></i>Submit Another Event
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
:root {
    --primary-color: #730000;
    --primary-dark: #5c0000;
    --primary-hover: #8a0000;
    --secondary-color: #f5f5f5;
    --text-color: #333;
    --text-muted: #666;
    --success-color: #28a745;
    --border-color: #e0e0e0;
    --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    --shadow-hover: 0 8px 15px rgba(0, 0, 0, 0.15);
}

.success-header {
    background: linear-gradient(135deg, var(--secondary-color), #ffffff);
    border-bottom: 1px solid var(--border-color);
}

.success-icon-container {
    margin-bottom: 1.5rem;
}

.success-icon {
    font-size: 5rem;
    color: var(--success-color);
    animation: successPulse 2s ease-in-out infinite;
}

@keyframes successPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}

.success-title {
    color: var(--primary-color);
    font-weight: 700;
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
}

.success-subtitle {
    color: var(--text-muted);
    font-size: 1.2rem;
    margin-bottom: 0;
}

.success-card {
    background: white;
    border-radius: 16px;
    box-shadow: var(--shadow-hover);
    overflow: hidden;
    animation: slideUp 0.6s ease-out;
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.success-content {
    padding: 3rem;
}

.success-message {
    text-align: center;
    margin-bottom: 3rem;
}

.message-title {
    color: var(--primary-color);
    font-weight: 700;
    font-size: 2rem;
    margin-bottom: 1rem;
}

.message-text {
    color: var(--text-color);
    font-size: 1.2rem;
    font-weight: 500;
    margin-bottom: 1rem;
    line-height: 1.6;
}

.message-subtext {
    color: var(--text-muted);
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: 0;
}

.next-steps {
    margin-bottom: 3rem;
}

.steps-title {
    color: var(--primary-color);
    font-weight: 600;
    font-size: 1.5rem;
    margin-bottom: 2rem;
    text-align: center;
}

.steps-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.step-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem;
    background: var(--secondary-color);
    border-radius: 12px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.step-item:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow);
}

.step-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    background: var(--primary-color);
    color: white;
    border-radius: 50%;
    font-size: 1.2rem;
    flex-shrink: 0;
}

.step-content h4 {
    color: var(--primary-color);
    font-weight: 600;
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
}

.step-content p {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.5;
    margin-bottom: 0;
}

.action-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
}

.btn {
    border-radius: 8px;
    padding: 1rem 2rem;
    font-weight: 600;
    transition: all 0.3s ease;
    border: none;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    font-size: 1.1rem;
}

.btn-primary {
    background: linear-gradient(135deg, var(--primary-color), var(--primary-hover));
    color: white;
}

.btn-primary:hover {
    background: linear-gradient(135deg, var(--primary-dark), var(--primary-color));
    transform: translateY(-2px);
    box-shadow: var(--shadow);
    color: white;
    text-decoration: none;
}

.btn-outline-primary {
    border: 2px solid var(--primary-color);
    color: var(--primary-color);
    background: transparent;
}

.btn-outline-primary:hover {
    background: var(--primary-color);
    color: white;
    transform: translateY(-2px);
    text-decoration: none;
}

@media (max-width: 768px) {
    .success-title {
        font-size: 2rem;
    }
    
    .success-content {
        padding: 2rem;
    }
    
    .action-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .btn {
        width: 100%;
        max-width: 300px;
        justify-content: center;
    }
    
    .steps-list {
        gap: 1rem;
    }
    
    .step-item {
        padding: 1rem;
    }
}
</style>
