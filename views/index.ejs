
    <!-- Hero Section -->
    <section class="hero-section py-5">
        <div class="container">
            <div class="row justify-content-center text-center">
                <div class="col-lg-8">
                    <h1 class="hero-title mb-4">
                        Join the Fun at<br>
                        <span class="text-primary">Cola's Club!</span>
                    </h1>
                    <p class="hero-subtitle mb-5">
                        Connect, Create, Celebrate – Your Columbia Adventure Awaits!
                    </p>
                    <div class="hero-buttons d-flex flex-column flex-sm-row gap-3 justify-content-center align-items-center">
                        <a href="/signup" class="btn btn-primary btn-lg px-5 py-3">
                            Start Your Adventure
                        </a>
                        <a href="/events" class="btn btn-secondary btn-lg px-5 py-3">
                            Browse Events
                        </a>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Floating Elements -->
        <div class="floating-element floating-1"></div>
        <div class="floating-element floating-2"></div>
        <div class="floating-element floating-3"></div>
        <div class="floating-element floating-4"></div>
        <div class="floating-element floating-5"></div>
        <div class="floating-element floating-6"></div>
        <div class="floating-element floating-7"></div>
        <div class="floating-element floating-8"></div>
    </section>

    <!-- Event Types Showcase -->
    <section class="event-types-showcase py-5">
        <div class="container">
            <div class="row text-center mb-5">
                <div class="col-12">
                    <h2 class="section-title">
                        Create and Join in Adventures
                    </h2>
                    <p class="section-subtitle">From outdoor exploration to social connections, find activities that match your interests</p>
                </div>
            </div>

            <div class="row g-3 justify-content-center">
                <!-- Hiking & Trails -->
                <div class="col-md-3 col-lg-2">
                    <div class="event-type-showcase-card hiking">
                        <div class="event-type-icon">
                            <i class="fas fa-hiking"></i>
                        </div>
                        <h4>Hiking & Trails</h4>
                    </div>
                </div>

                <!-- Water Adventures -->
                <div class="col-md-3 col-lg-2">
                    <div class="event-type-showcase-card water">
                        <div class="event-type-icon">
                            <i class="fas fa-water"></i>
                        </div>
                        <h4>Water Adventures</h4>
                    </div>
                </div>

                <!-- Social Events -->
                <div class="col-md-3 col-lg-2">
                    <div class="event-type-showcase-card social">
                        <div class="event-type-icon">
                            <i class="fas fa-users"></i>
                        </div>
                        <h4>Social Gatherings</h4>
                    </div>
                </div>

                <!-- Sports & Fitness -->
                <div class="col-md-3 col-lg-2">
                    <div class="event-type-showcase-card sport">
                        <div class="event-type-icon">
                            <i class="fas fa-running"></i>
                        </div>
                        <h4>Sports & Fitness</h4>
                    </div>
                </div>

                <!-- Cycling Adventures -->
                <div class="col-md-3 col-lg-2">
                    <div class="event-type-showcase-card bike">
                        <div class="event-type-icon">
                            <i class="fas fa-biking"></i>
                        </div>
                        <h4>Cycling</h4>
                    </div>
                </div>

                <!-- Special Events -->
                <div class="col-md-3 col-lg-2">
                    <div class="event-type-showcase-card special">
                        <div class="event-type-icon">
                            <i class="fas fa-star"></i>
                        </div>
                        <h4>Special Events</h4>
                    </div>
                </div>
            </div>

            <div class="row text-center mt-4">
                <div class="col-12">
                    <a href="/events" class="btn btn-primary btn-lg px-5 py-3">
                        Browse All Events
                    </a>
                    <a href="/submit_event" class="btn btn-secondary btn-lg px-5 py-3">
                        Create Your Own Event
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section py-5 bg-light">
        <div class="container">
            <div class="row justify-content-center text-center">
                <div class="col-lg-8">
                    <h2 class="section-title">
                        Ready to <span class="text-primary">Connect</span>?
                    </h2>
                    <p class="section-subtitle mb-5">
                        Join thousands of community members who are already making meaningful connections and creating unforgettable memories in Columbia.
                    </p>
                    <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center">
                        <a href="/signup" class="btn btn-primary btn-lg px-5 py-3">
                            Create Your Account
                        </a>
                        <a href="/contact" class="btn btn-secondary btn-lg px-5 py-3">
                            Learn More
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <style>
    /* ========================================
       INDEX PAGE SPECIFIC STYLES
       ======================================== */

    /* Hero Section */
    .hero-section {
        background: linear-gradient(135deg, rgba(115, 0, 0, 0.05) 0%, rgba(248, 249, 250, 1) 50%, rgba(115, 0, 0, 0.05) 100%);
        position: relative;
        overflow: hidden;
    }

    .hero-title {
        font-size: 3.5rem;
        font-weight: 900;
        line-height: 1.1;
        color: var(--text-primary);
    }

    .hero-subtitle {
        font-size: 1.25rem;
        color: var(--text-secondary);
        max-width: 600px;
        margin: 0 auto;
    }

    .text-primary {
        color: var(--primary-color) !important;
    }

    .text-accent {
        color: var(--secondary-text) !important;
    }

    /* Floating Elements */
    .floating-element {
        position: absolute;
        border-radius: 50%;
        opacity: 0.4;
        animation: float 3s ease-in-out infinite;
    }

    .floating-1 {
        width: 64px;
        height: 64px;
        background: rgba(115, 0, 0, 0.3);
        top: 20%;
        left: 10%;
        animation-delay: 0s;
    }

    .floating-2 {
        width: 48px;
        height: 48px;
        background: rgba(107, 114, 128, 0.4);
        top: 40%;
        right: 20%;
        animation-delay: 1s;
    }

    .floating-3 {
        width: 32px;
        height: 32px;
        background: rgba(31, 41, 55, 0.5);
        bottom: 20%;
        left: 25%;
        animation-delay: 2s;
    }

    .floating-4 {
        width: 56px;
        height: 56px;
        background: rgba(115, 0, 0, 0.25);
        top: 15%;
        right: 35%;
        animation-delay: 0.5s;
    }

    .floating-5 {
        width: 40px;
        height: 40px;
        background: rgba(107, 114, 128, 0.35);
        bottom: 35%;
        right: 15%;
        animation-delay: 1.5s;
    }

    .floating-6 {
        width: 72px;
        height: 72px;
        background: rgba(31, 41, 55, 0.3);
        top: 60%;
        left: 5%;
        animation-delay: 2.5s;
    }

    .floating-7 {
        width: 36px;
        height: 36px;
        background: rgba(115, 0, 0, 0.4);
        bottom: 15%;
        left: 60%;
        animation-delay: 0.8s;
    }

    .floating-8 {
        width: 52px;
        height: 52px;
        background: rgba(107, 114, 128, 0.45);
        top: 25%;
        left: 70%;
        animation-delay: 1.8s;
    }

    @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-20px) rotate(5deg); }
    }

    /* Buttons */
    .btn-primary {
        background: var(--primary-color);
        border-color: var(--primary-color) !important;
        border-width: 2px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-primary:hover {
        background: var(--primary-hover);
        border-color: var(--primary-hover) !important;
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    .btn-outline-primary {
        color: var(--primary-color) !important;
        border-color: var(--primary-color) !important;
        border-width: 2px;
        background: transparent;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-outline-primary:hover {
        background: var(--primary-color) !important;
        border-color: var(--primary-color) !important;
        color: var(--white) !important;
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    .btn-secondary {
        background: var(--secondary-color);
        border-color: var(--primary-color) !important;
        border-width: 2px;
        color: var(--text-primary);
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-secondary:hover {
        background: var(--secondary-dark);
        border-color: var(--primary-hover) !important;
        color: var(--text-primary);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    .btn-outline-accent {
        border-color: var(--secondary-text) !important;
        border-width: 2px;
        color: var(--secondary-text);
        background: transparent;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-outline-accent:hover {
        background: var(--secondary-text);
        border-color: var(--secondary-text) !important;
        color: var(--white);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    .btn-accent {
        background: var(--secondary-text);
        border-color: var(--primary-color) !important;
        border-width: 2px;
        color: var(--white);
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-accent:hover {
        background: var(--secondary-dark);
        border-color: var(--primary-hover) !important;
        color: var(--text-primary);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    /* Section Titles */
    .section-title {
        font-size: 2.5rem;
        font-weight: 900;
        color: var(--text-primary);
        margin-bottom: 1rem;
    }

    .section-subtitle {
        font-size: 1.1rem;
        color: var(--text-secondary);
        margin-bottom: 3rem;
    }

    /* Feature Cards */
    .feature-card {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        text-align: center;
        box-shadow: var(--shadow);
        transition: all 0.3s ease;
        height: 100%;
        border: 2px solid transparent;
    }

    .feature-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--shadow-hover);
        border-color: rgba(243, 156, 18, 0.3);
    }

    .feature-icon {
        width: 64px;
        height: 64px;
        background: rgba(115, 0, 0, 0.1);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        font-size: 1.5rem;
        color: var(--primary-color);
        transition: all 0.3s ease;
    }

    .feature-icon.accent {
        background: rgba(243, 156, 18, 0.1);
        color: var(--accent-color);
    }

    .feature-card:hover .feature-icon {
        background: rgba(115, 0, 0, 0.2);
        transform: scale(1.1);
    }

    .feature-card:hover .feature-icon.accent {
        background: rgba(243, 156, 18, 0.2);
    }

    .feature-card h3 {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 1rem;
    }

    .feature-card p {
        color: var(--text-secondary);
        line-height: 1.6;
        margin: 0;
    }

    /* Stats Section */
    .stats-section {
        background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
        color: white;
    }

    .stat-item {
        padding: 1rem;
    }

    .stat-number {
        font-size: 2.5rem;
        font-weight: 900;
        margin-bottom: 0.5rem;
    }

    .stat-label {
        font-size: 1rem;
        opacity: 0.9;
    }

    /* Event Types Showcase */
    .event-types-showcase {
        background: var(--secondary-color);
    }

    .event-type-showcase-card {
        background: var(--white);
        border-radius: 12px;
        padding: 1.5rem 1rem;
        text-align: center;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        height: 100%;
        border-top: 4px solid var(--general-color);
        position: relative;
        overflow: hidden;
    }

    .event-type-showcase-card.hiking {
        border-top-color: #dc3545;
    }

    .event-type-showcase-card.water {
        border-top-color: #0d6efd;
    }

    .event-type-showcase-card.social {
        border-top-color: #6f42c1;
    }

    .event-type-showcase-card.sport {
        border-top-color: #fd7e14;
    }

    .event-type-showcase-card.bike {
        border-top-color: #20c997;
    }

    .event-type-showcase-card.special {
        border-top-color: var(--secondary-text);
    }

    .event-type-showcase-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    .event-type-icon {
        width: 60px;
        height: 60px;
        background: var(--secondary-color);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem;
        font-size: 1.5rem;
        color: var(--text-primary);
        transition: all 0.3s ease;
    }

    .event-type-showcase-card:hover .event-type-icon {
        transform: scale(1.1);
    }

    .event-type-showcase-card h4 {
        font-size: 1.1rem;
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: 0.75rem;
    }

    .event-type-features {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
        align-items: center;
    }

    .feature-tag {
        background: var(--secondary-color);
        color: var(--text-secondary);
        padding: 0.2rem 0.5rem;
        border-radius: 12px;
        font-size: 0.7rem;
        font-weight: 500;
        width: 100%;
        text-align: center;
    }

    /* Footer */
    .footer {
        background: var(--text-primary) !important;
    }

    .footer-link {
        color: var(--text-secondary);
        text-decoration: none;
        transition: color 0.3s ease;
        display: block;
        margin-bottom: 0.5rem;
    }

    .footer-link:hover {
        color: var(--accent-color);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .hero-title {
            font-size: 2.5rem;
        }
        
        .section-title {
            font-size: 2rem;
        }
        
        .hero-buttons {
            flex-direction: column;
        }
        
        .btn {
            width: 100%;
            max-width: 300px;
        }

        .event-type-showcase-card {
            margin-bottom: 1rem;
        }
    }
    </style>
